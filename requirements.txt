gradio>=6.10.0
soundfile>=0.13.0
torch>=2.0.0
torchaudio>=2.0.0
qwen-tts>=0.1.1
transformers>=4.50.0
accelerate>=1.0.0
librosa>=0.11.0

# Optional — install separately for faster TTS inference on CUDA:
# pip install flash-attn --no-build-isolation
